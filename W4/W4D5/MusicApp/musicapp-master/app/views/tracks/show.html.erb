<h2> <%= @track.name %> </h2>
<br>
<p>Album: <%= @track.album.title %></p>
<br>
<p>Lyrics:</p>
<br>
<p><%= @track.lyrics %> </p>
<br>
<p><%= @track.is_bonus ? "Bonus Track!" : "" %></p>
<br>

  <%= render 'notes/form', track: @track %>

<br>

<p><%= render @track.notes %></p>

<br>
<%= link_to 'Edit track', edit_track_url %>
<br>
<%= link_to 'Delete track', track_url(@track), method: :delete %>
<br>
<%= link_to "#{@track.album.title}", album_url(@track.album) %>
<br>
<%= link_to 'Add a new track', new_album_track_url(@track.album) %>
<br>
